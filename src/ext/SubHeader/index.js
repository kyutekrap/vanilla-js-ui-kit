import { SubHeaderBase } from "vanilla-js-ui-kit";

export class SubHeader extends SubHeaderBase {
    constructor(text = "") {
        super(text);
        this.initialize();
    }

    initialize() {
        super.initialize();
    }
}