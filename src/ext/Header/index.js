import { HeaderBase } from "../../../base/index.js";

export class Header extends HeaderBase {
    constructor(text = "") {
        super(text);
        this.initialize();
    }

    initialize() {
        super.initialize();
    }
}