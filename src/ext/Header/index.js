import { HeaderBase } from "vanilla-js-ui-kit";

export class Header extends HeaderBase {
    constructor(text = "") {
        super(text);
        this.initialize();
    }

    initialize() {
        super.initialize();
    }
}