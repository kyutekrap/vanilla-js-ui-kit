import { SpanBase } from "../../../base/index.js";

export class Span extends SpanBase {
    constructor(text = "") {
        super(text);
    }

    initialize() {
        super.initialize();
    }
}