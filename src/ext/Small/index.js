import { SmallBase } from "vanilla-js-ui-kit";

export class Small extends SmallBase {
    constructor(text = "") {
        super(text);
        this.initialize();
    }

    initialize() {
        super.initialize();
    }
}