import { LoaderProps } from "../../props";
import { LoaderBase } from "../../base";

export class LoaderDefault extends LoaderBase {
    constructor(props: LoaderProps = {}) {
        super(props);
    }
}