import { SectionBase } from "../../base";
import { SectionProps } from "../../props";

export class SectionDefault extends SectionBase {
    constructor(props: SectionProps) {
        super(props);
    }
}