import { FlexBoxBase } from "../../base";
import { FlexBoxProps } from "../../props";

export class FlexBoxDefault extends FlexBoxBase {
    constructor(props: FlexBoxProps) {
        super(props);
    }
}