import { AppBarBase } from "../../base/AppBar";
import { AppBarProps } from "../../props/AppBar";

export class AppBarDefault extends AppBarBase {
    constructor(props: AppBarProps) {
        super(props);
    }
}