import { LineBase } from "../../base/Line";
import { LineProps } from "../../props/Line";

export class LineDefault extends LineBase {
    constructor(props: LineProps) {
        super(props);
    }
}